<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/css/reset.css" rel="stylesheet">
    <link href="/css/paginaListaAdministrador.css" rel="stylesheet">
    <title>Gestão - Alunos</title>

    <style>
        /* Estilos do modal */
        .modal {
            display: none; /* Oculta o modal por padrão */
            position: fixed; /* Posicionamento fixo */
            z-index: 1; /* Define a ordem de empilhamento */
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            overflow: auto; /* Habilita a rolagem caso o conteúdo do modal seja maior que a tela */
            background-color: rgba(0, 0, 0, 0.4); /* Cor de fundo semi-transparente */
            border-radius: 25px;

        }

        .modal-content {
            background-color: #FFFFFF;
            margin: 10% auto; /* Centraliza o modal verticalmente */
            padding: 20px;
            border: 15px solid #9B0303;
            width: 80%;
            max-width: 500px;
            border-radius: 25px;

        }

        .close {
            color: #aaa;
            float: right;
            font-size: 28px;
            font-weight: bold;
        }

        .close:hover,
        .close:focus {
            color: black;
            text-decoration: none;
            cursor: pointer;
        }

        .input-container {
            margin-bottom: 10px;
        }

        .input-container label {
            display: block;
            font-weight: bold;
            margin-bottom: 5px;
        }

        .input-container input {
            width: 100%;
            padding: 5px;
            border-radius: 20px;
            background-color: #E7E7E7;
        }

        .input-container select {
            width: 100%;
            padding: 5px;
            border-radius: 20px;
        }

        .modal-buttons {
            text-align: right;
        }

        .modal-buttons button {
            margin-left: 5px;
            border-radius: 20px;
            background-color: #9B0303;
        }

        .input-container {
            display: flex;
            justify-content: space-between;
        }

        .modal-buttons {
            display: flex;
            justify-content: flex-end;
            border-radius: 20px;
        }

        .modal-buttons button:first-child {
            margin-right: 5px;
        }


    </style>
</head>

<body class="fade-in">
    <%- include('partials/backButton.ejs') %>
    <div class="container">
        <div class="topRow">
            <%- include('partials/searchBox.ejs') %>
            <button class="addButton" onclick="openModal()">Adicionar aluno ➕</button>
        </div>
        <div class="objectListWrapper">
            <% if (!alunos || Object.keys(alunos).length===0) { %>
                <p>Ops! Parece que não existe nenhum aluno cadastrado ainda.</p>
            <% } else { %>
                <% for (i = 0; i < alunos.length; i++) { %>
                    <% let aluno = alunos[i] %>
                    <div class="objectListItem">
                        <span class="span-row span-row-large">
                            <%= aluno.nome %>
                        </span>
                        <span class="span-row span-row-medium">
                            <%= aluno.ra %>
                        </span>
                        <span class="span-row span-row-large">
                            <%= aluno.Curso.nome %>
                        </span>
                        <div class="flex-end-buttons-row">
                            <button class="flex-end-button">🗑️</button>
                            <button class="flex-end-button">✏️</button>
                        </div>
                    </div>
                <% } %>
            <% } %>
        </div>
    </div>

    <!-- O Modal -->
<div id="myModal" class="modal">
    <!-- Conteúdo do modal -->
    <div class="modal-content">
        <span class="close" onclick="closeModal()">&times;</span>
        <h3>Adicionar Aluno</h3>


        <!-- Nome -->
        <div class="input-container">
            <input type="text" id="nome" name="nome" value="Nome">
        </div>

        <!-- RA -->
        <div class="input-container">
            <div style="display: flex; justify-content: space-between;">
                <div style="width: 48%;">
                <input type="text" id="ra" name="ra" value="RA">
                </div>
        <!-- EMAIL -->
                <div style="width: 48%;">
                    <input type="email" id="email" name="email" value="E-mail">
                </div>
            </div>
        </div>

        <!-- CURSO -->
        <div class="input-container">
            <select id="curso" name="curso" value="Curso">
                <option value="ingles">Inglês</option>
                <option value="espanhol" selected>Espanhol</option>
            </select>
        <!-- TERMO -->
        <input type="text" id="termo" name="termo" value="Termo">
        </div>

        <div class="modal-buttons">
            <button onclick="adicionarAluno()">Adicionar</button>
            <button onclick="closeModal()">Cancelar</button>
        </div>
    </div>
</div>


    <script>
        // Função para abrir o modal
        function openModal() {
            document.getElementById("myModal").style.display = "block";
        }

        // Função para fechar o modal
        function closeModal() {
            document.getElementById("myModal").style.display = "none";
        }

        // Função para adicionar aluno
        function adicionarAluno() {
            // Obter os valores dos campos do formulário
            var nome = document.getElementById("nome").value;
            var ra = document.getElementById("ra").value;
            var email = document.getElementById("email").value;
            var curso = document.getElementById("curso").value;
            var termo = document.getElementById("termo").value;

            // Fazer algo com os valores (enviar para o servidor, por exemplo)
            // ...

            // Fechar o modal após adicionar o aluno
            closeModal();
        }
    </script>
</body>

</html>
